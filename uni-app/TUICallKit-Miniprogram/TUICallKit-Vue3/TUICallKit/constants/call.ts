export enum CallStatus {
    IDLE = 'idle',
    CALLING = 'calling',
    CONNECTED = 'connected',
}

export enum CallRole {
    UNKNOWN = 'unknown',
    CALLEE = 'callee',
    CALLER = 'caller',
}

export enum DeviceStatus {
    UNKNOWN = 'unknown',
    ON = 'on',
    OFF = 'off',
}
